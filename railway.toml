# Railway Configuration for 24Rx Platform

[build]
builder = "dockerfile"
dockerfilePath = "backend/Dockerfile"

[deploy]
numReplicas = 1
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
