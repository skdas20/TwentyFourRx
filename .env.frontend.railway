# Frontend Environment Variables for Railway
# Copy and paste these into Railway Dashboard → Frontend Service → Variables tab

NODE_ENV=production
NEXT_PUBLIC_API_URL=https://${{backend.RAILWAY_PUBLIC_DOMAIN}}/api/v1
